import { Bal, Product, Transaction } from "../types";

// Mock data dikosongkan — semua data sekarang disimpan di Supabase
export const initialBals: Bal[] = [];
export const initialProducts: Product[] = [];
export const initialTransactions: Transaction[] = [];
